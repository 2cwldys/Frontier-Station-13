pypresence>=4.3.0
pywin32>=306
psutil>=5.9.0
python-dotenv>=1.0.0
# Tray icon -- the only UI a --noconsole background build has. Optional at
# runtime (tray.py degrades to headless if these are absent), required to build
# the distributable.
pystray>=0.19.0
Pillow>=10.0.0
